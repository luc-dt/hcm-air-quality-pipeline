name: 'hcm_air_quality'
version: '1.0.0'
profile: 'hcm_air_quality'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:         # directories to be removed by `dbt clean`
  - "target"
  - "dbt_packages"

models:
  hcm_air_quality:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
